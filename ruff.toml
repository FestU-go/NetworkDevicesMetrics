line-length = 120
target-version = "py313"

exclude = [
    ".venv",
    "venv",
    ".env",
    "__pycache__",
]

[lint]
select = ["E", "W", "F", "I", "UP", "B", "C4", "SIM"]
ignore = ["E501", "B008"]
fixable = ["ALL"]
